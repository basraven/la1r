---
name: k8s
description: "Invoke the Kubernetes Manifest Engineer agent for cluster tasks"
usage: "/k8s [your request]"
---
Use the `agent` tool to delegate the following task to the `kubernetes-manifest-engineer` agent:

{{args}}

Return the summary provided by the agent to the main session once the task is complete.