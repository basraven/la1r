apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
resources:
  - gatus.yml
configMapGenerator:
  - name: gatus-config
    files:
      - config/config.yaml
  - name: sablier-proxy-src
    files:
      - sablier-proxy/main.go
