# ---
# Source: https://www.digitalocean.com/community/tutorials/how-to-set-up-an-elasticsearch-fluentd-and-kibana-monitoring-logging-stack-on-kubernetes
# kind: PersistentVolumeClaim
# apiVersion: v1
# metadata:
#   name: kibana-claim
#   namespace: monitoring
# spec:
#   accessModes:
#   - ReadWriteMany 
#   storageClassName: local-storage
#   selector:
#     matchLabels:
#       contenttype: "kibana"
#   resources:
#     requests:
#       storage: 200Gi
# ---
# kind: PersistentVolumeClaim
# apiVersion: v1
# metadata:
#   name: kibana-config-claim-local
#   namespace: monitoring
# spec:
#   accessModes:
#   - ReadWriteMany 
#   storageClassName: local-storage
#   selector:
#     matchLabels:
#       contenttype: "kibana-config-local"
#   resources:
#     requests:
#       storage: 20Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kibana
  namespace: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      k8s-app: kibana
  template:
    metadata:
      labels:
        task: monitoring
        k8s-app: kibana
    spec:
      containers:
      - name: kibana
        resources: {}
        ports:
        - containerPort: 5601 
        env:
          - name: ELASTICSEARCH_URL
            value: http://elasticsearch.monitoring:9200
          - name: ELASTICSEARCH_HOSTS
            value: http://elasticsearch.monitoring:9200
        image: kibana:7.9.1
        # volumeMounts:
        #   - name: kibana
        #     mountPath: /home/coder/project
        #   - name: kibana-config-local
        #     mountPath: /home/coder/.local
        #   - name: kibana-config-vscode
        #     mountPath: /home/coder/.vscode
        #   - name: kibana-init
        #     mountPath: /seb/init.sh
        #     subPath: init.sh
      # volumes:
      #   - name: kibana
      #     persistentVolumeClaim:
      #       claimName: kibana-claim
      #   - name: kibana-config-local
      #     persistentVolumeClaim:
      #       claimName: kibana-config-claim-local
      #   - name: kibana-config-vscode
      #     persistentVolumeClaim:
      #       claimName: kibana-config-claim-vscode
      #   - name: kibana-init
      #     configMap:
      #       name: kibana-init
      #       items:
      #       - key: init.sh
      #         path: init.sh
      #       defaultMode: 0777
---
apiVersion: v1
kind: Service
metadata:
  labels:
    task: monitoring
    kubernetes.io/cluster-service: 'true'
    kubernetes.io/name: kibana
  name: kibana
  namespace: monitoring
spec:
  ports:
  - port: 80
    targetPort: 5601
  selector:
    k8s-app: kibana
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: kibana
  namespace: monitoring
spec:
  rules:
  - host: kibana.bas
    http:
      paths:
      - path: /
        backend:
          serviceName: kibana
          servicePort: 80

  tls:
  - hosts:
    - kibana.bas