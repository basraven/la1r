apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: registry
resources:
  - container-build.yml
  - deployment.yml
configMapGenerator:
- name: example-hello-dockerfile
  files:
  - Dockerfile
generatorOptions:
  disableNameSuffixHash: true
