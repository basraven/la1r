apiVersion: v1
kind: ConfigMap
metadata:
  name: workflow-controller-configmap
  namespace: argo
data:
  config: |
    artifactRepository:
      archiveLogs: false

    workflowDefaults:
      spec:
        serviceAccountName: argo-workflow-controller
