apiVersion: argoproj.io/v1alpha1
kind: CronWorkflow
metadata:
  name: weekly-immich-db-cloud-backup
spec:
  schedule: "30 12 * * 0" # Every Sunday at 12:30 AM
  concurrencyPolicy: "Forbid"
  timezone: "Europe/Amsterdam"
  workflowSpec:
    workflowTemplateRef:
      name: nonessential-cloud-backup
    arguments:
      parameters:
        - name: appname
          value: "immich"
        - name: localpath
          value: "/mnt/ssd/ha/immich/data/backups"
