apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- namespace.yml
# - middleware.yml
- kubernetes-dashboard-ingress.yml
- kubernetes-dashboard-admin.yml
- dashboard-sso.yml
- dashboard-sso-token.yml # use this to extract token: kubectl -n kubernetes-dashboard get secret dashboard-sso-token -o jsonpath='{.data.token}' | base64 -d; echo
