---
apiVersion: batch/v1
kind: Job
metadata:
  name: claudecodeui-build-image
  namespace: development
  labels:
    app: claudecodeui
    contenttype: "build-job"
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: claudecodeui
        job: build-image
    spec:
      restartPolicy: Never
      containers:
      - name: kaniko
        image: gcr.io/kaniko-project/executor:latest
        args:
        - --dockerfile=Dockerfile
        - --context=/workspace
        - --destination=registry.registry.svc.cluster.local:5000/claudecodeui:v5
        - --skip-tls-verify-registry=registry.registry.svc.cluster.local:5000
        - --cache=true
        - --single-snapshot
        volumeMounts:
        - name: dockerfile
          mountPath: /workspace/Dockerfile
          subPath: Dockerfile
      volumes:
      - name: dockerfile
        configMap:
          name: claudecodeui-dockerfile
          items:
          - key: Dockerfile
            path: Dockerfile
