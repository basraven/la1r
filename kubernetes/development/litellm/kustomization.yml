apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: development
resources:
  - litellm.yml
configMapGenerator:
- name: litellm-config
  files:
  - config.yaml
generatorOptions:
  disableNameSuffixHash: true
