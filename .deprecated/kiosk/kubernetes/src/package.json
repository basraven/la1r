{
  "name": "kitchen-kiosk",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "parcel src/index.html --port 3000 --host 0.0.0.0",
    "build": "parcel build src/index.html --public-url ./",
    "lint": "eslint src"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "parcel": "^2.10.0"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead"
}
