---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mqtt-data-claim
  namespace: homeautomation
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: mqtt-data
  selector:
    matchLabels:
      contenttype: "mqtt-data"
  resources:
    requests:
      storage: 150Mi